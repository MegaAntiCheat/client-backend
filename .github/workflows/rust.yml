name: Rust

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build-linux:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    
    - name: Debug
      run: cargo build --verbose
    - uses: actions/upload-artifact@v3
      with:
        name: client-linux
        path: target/debug/client-backend
      
      
  build-windows:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v3
    
    - name: Debug
      run: cargo build --verbose
    - uses: actions/upload-artifact@v3
      with:
        name: client-windows
        path: target/debug/client-backend.exe
